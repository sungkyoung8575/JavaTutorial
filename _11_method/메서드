메서드

1. 어떤 기능을 정의
2. 정의, 호출 구분 (메소드를 콜한다)
-정의(만든다)
접근제어자 리턴타입 메서드명(매개변수 정의)

-호출(call,사용)
메서드명(매개변수 값)

위 매개변수 정의와 매개변수값은 생략될 수 있다.

3. 메서드를 사용하는 흐름

call > 코드실행 > 리턴(호출 위치로 전달하는 값)

-그냥 호출
-무엇인가 넘겨주는