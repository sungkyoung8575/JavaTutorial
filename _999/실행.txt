소스
원시프로그램(.java) : 사람이 작성(사람이 개발중인소스) ->컴파일(기계어로변역)->목적프로그램(.class) : 기계가이해(실행목적)
				  
 프로그램 실행목적이라면 class 파일만 배포해도됨
 예) int a=30; -> 기계어로변경(0.1 조합)	// 개념만 가져가기
	          		-컴파일 
	          	
(ctrl F11) 컴파일후 실행

언어의 종류 : 컴파일(자바) vs 인터프리터(자바스크립트)
						-컴파일없이 바로 번역해서 실행
			  
보조기억장치(하드웨어?파일탐색기로 검색가능한?) , 주기억장치(메모리(RAM)

* 목적프로그램은 실행중인가? NO
 실행중=> 주기억장치에 상주한다//?
 
 
   ★thread (코드를 실행하는애, 목적프로그램을 실제 실행) / 가장먼저 실행되는건 메인메서드!
  프로그램 -> thread 로 쪼개서 ?실행 싱글스레드,멀티스레드 
  
 RunTime (실행) JVM *구조생각하기

 목적프로그램 ->클래스로드 -> JVM ( Method | Stack |  Heap | GC(가비지콜렉터))
 			-필요한소스코드
 			
 Method	- 소스파일, 멤버변수, *Static (공용변수)		공통영역
 Stack	- 메서드 call -> FILO 지역변수
 Heap	- 객체 (인스턴트) (생성,사용,소멸)				공통영역
 
 *Static (공용변수) - 객체를 만들지 않아도 접근가능
 
 
 final  초기값이 생성된 후는 바꿀수 없다
 
 
 접근제어자  스택틱  파이널   따로공부해!